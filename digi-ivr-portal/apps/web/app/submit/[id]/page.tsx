"use client";
import React from "react";

const SubmitPage = ({ params }: { params: { id: string } }) => {
  console.log(params);
  return <div>SubmitPage</div>;
};

export default SubmitPage;
